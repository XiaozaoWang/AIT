
import {config} from 'dotenv';
config();

// import './config.mjs' before any other imports
// what does it do?
// It reads the .env file and sets the environment variables
// can be accessed using process.env.VARIABLE_NAME